local old = CScreenStore.SelectStoreItem
CScreenStore.SelectStoreItem = function(self, nIndex, bSelected)
    if store.storeItems[nIndex + 1].item.res == "YF_cask2" then return end
    old(self, nIndex, bSelected)
end
